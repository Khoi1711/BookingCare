@model IEnumerable<WebApplication4.Models.Speciaty>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SpeciatyName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SpeciatyName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SpeciatyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SpeciatyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SpeciatyID })
        </td>
    </tr>
}

</table>
